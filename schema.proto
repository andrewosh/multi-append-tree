message Node {
  required string name = 1;
  optional bytes value = 2;
  optional bytes paths = 3;
}

message LinkNode {
  required bytes key = 1;
  required uint64 node = 2;
  optional uint64 version = 3;
  optional bool versioned = 4;
}
